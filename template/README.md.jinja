# {{ project_name }}

{{ project_description }}

## 构建

CMake 3.31+{% if use_vcpkg %}，[vcpkg](https://vcpkg.io/)（`VCPKG_ROOT`）{% endif %}。

```bash
cmake --list-presets
cmake --workflow --preset windows-x64-msvc-vs2022-debug
```

分步：`cmake --preset …` → `cmake --build --preset …`{% if enable_testing %} → `ctest --preset …`{% endif %} → `cpack --preset …`。

本机覆盖写 `CMakeUserPresets.json`（gitignore）。

## 结构

```
include/{{ project_name }}/   # 公开头  #include <{{ project_name }}/{{ modules[0] }}.hpp>
src/<模块>/                  # 实现
apps/<程序>/                 # 入口
{% if enable_testing %}tests/Unit/<模块>/         # 单测
tests/Integration/           # 集成测试
{% endif %}```

加库：`include/{{ project_name }}/Foo.hpp`、`src/Foo/`，再在 `src/CMakeLists.txt` 里 `add_subdirectory(Foo)`{% if enable_testing %}；单测复制 `tests/Unit/{{ modules[0] }}/`{% endif %}。

加程序：复制 `apps/{{ apps[0] }}/`，在 `apps/CMakeLists.txt` 里 `add_subdirectory`。
{% if use_vcpkg %}

## 依赖

```bash
"$VCPKG_ROOT/vcpkg" add port fmt
"$VCPKG_ROOT/vcpkg" x-update-baseline
```

`vcpkg.json` / `vcpkg-configuration.json` 进 git。CI 需要完整克隆 vcpkg，浅克隆可能对不上 baseline。
{% endif %}
{% if license != 'none' %}
{% if license == 'mit' %}
## 许可

MIT。见 `LICENSE`。
{% elif license == 'apache-2.0' %}
## 许可

Apache-2.0。见 `LICENSE`。
{% elif license == 'bsd-3-clause' %}
## 许可

BSD-3-Clause。见 `LICENSE`。
{% elif license == 'gpl-3.0' %}
## 许可

GPL-3.0。见 `LICENSE`。
{% elif license == 'unlicense' %}
## 许可

Unlicense。见 `LICENSE`。
{% endif %}
{% endif %}
